#include "main.h"
 
/**
 * print_line -draws a straight line in the terminal
 * @c: number of time the character - should be printed
 /*
void print_line(int n)
{

     if (n <= 0)
     {
              putchar("\n");
      } else
      {

               int i:
          
                for (i =1; i <= n; i++)
               {
                   putchar("_");
                }
                 _putchar7-print_diagonal.c
 }

